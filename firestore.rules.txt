rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /contact_messages/{messageId} {
  allow create: if true;
}
    // Bu kural, herkese okuma ve yazma izni verir (SADECE TEST İÇİN)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}